import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Cores Principais (Azul)
  static const Color primary = Color(0xFF5E93AD);
  static const Color primaryDark = Color(0xFF467189);

  // Cores Secundárias (Laranja)
  static const Color secondary = Color(0xFFFF9900);
  static const Color secondaryDark = Color(0xFFCC7A00);

  // Cores Neutras e de Fundo
  static const Color background = Color(0xFFF8F9FA);
  static const Color white = Colors.white;

  // Cores de Texto e Status
  static const Color textTitle = Colors.black;
  static const Color textBody = Colors.grey;
  static const Color error = Colors.red;
  static const Color success = Colors.green;
}
